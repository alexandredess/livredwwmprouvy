{% extends 'base.html.twig' %}

{% block title %}Les Auteurs{% endblock %}

{% block body %}

<h1>Liste des Auteurs</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Prénom</th>
            
            
        </tr>
    </thead>
    <tbody>
        {% for auteur in auteurs %}
        <tr>
            <td>{{ auteur.id }}</td>
            <td>{{ auteur.nom }}</td>
            <td>{{ auteur.prenom }}</td>
            
        </tr>
        {% endfor %}
    </tbody>

{% endblock %}
